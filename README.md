# aus-pokies-profit-db
This repo will ETL a database containing the profit performance of NSWs pokies machines by LGA. There are options to standup a FastAPI to query the DB from the internet.

update - need to check connection is working.
